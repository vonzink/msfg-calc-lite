<div class="calc-page">
  <div class="calc-page__header">
    <h1>Schedule F Farm Income Calculator</h1>
    <p>Calculate monthly qualifying income from farm operations reported on IRS Schedule F (Form 1040). Add back non-cash expenses and apply standard averaging policy.</p>
  </div>

  <div class="calc-page__body">

    <!-- Upload Zone -->
    <div class="calc-section">
      <h2>Upload Schedule F</h2>
      <p class="text-muted text-sm" style="margin-bottom: var(--space-sm);">Upload IRS Schedule F pages to auto-fill fields. Upload one per tax year (max 2).</p>

      <div class="upload-zone" data-upload-type="income-schedule-f">
        <div class="upload-zone__icon">
          <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
        </div>
        <div class="upload-zone__text">
          <strong>Upload Schedule F</strong>
          <span>Drop or click to upload a Schedule F image or PDF</span>
        </div>
        <input type="file" class="upload-zone__input" accept="image/*,application/pdf" style="display: none;">
        <div class="upload-zone__status"></div>
      </div>

      <!-- JS-rendered document cards -->
      <div id="docCards"></div>
    </div>

    <!-- Policy Explanation -->
    <div class="calc-section">
      <div class="policy-box">
        <h5>Farm Income Policy</h5>
        <p>
          Farm income from Schedule F is calculated by starting with net profit or loss, adding back
          non-cash expenses (depreciation, amortization, casualty loss, depletion, business use of home),
          including ongoing cooperative and CCC payments, and subtracting the non-deductible portion of
          meals and entertainment. If two years are provided and Year 1 exceeds Year 2, income is
          averaged over 24 months. Otherwise, only the most recent year is used.
        </p>
      </div>
    </div>

    <!-- Farm Income Line Items -->
    <div class="calc-section">
      <h2>Farm Income</h2>
      <p class="text-muted text-sm" style="margin-bottom: var(--space-sm);">Enter line items from Schedule F. Positive values are added; negative values reduce income.</p>

      <table class="income-table">
        <thead>
          <tr>
            <th>Description</th>
            <th>Sign</th>
            <th>Year 1 (Most Recent)</th>
            <th>Year 2 (Prior)</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Net Profit or Loss</td>
            <td class="sign-col add">+/&minus;</td>
            <td><input type="number" id="f_np1" value="0" oninput="calculate()"></td>
            <td><input type="number" id="f_np2" value="0" oninput="calculate()"></td>
          </tr>
          <tr>
            <td>Ongoing Coop &amp; CCC Payments</td>
            <td class="sign-col add">+</td>
            <td><input type="number" id="f_coop1" value="0" oninput="calculate()"></td>
            <td><input type="number" id="f_coop2" value="0" oninput="calculate()"></td>
          </tr>
          <tr>
            <td>Other Income or Loss (non-recurring)</td>
            <td class="sign-col add">+/&minus;</td>
            <td><input type="number" id="f_other1" value="0" oninput="calculate()"></td>
            <td><input type="number" id="f_other2" value="0" oninput="calculate()"></td>
          </tr>
          <tr>
            <td>Depreciation</td>
            <td class="sign-col add">+</td>
            <td><input type="number" id="f_dep1" value="0" oninput="calculate()"></td>
            <td><input type="number" id="f_dep2" value="0" oninput="calculate()"></td>
          </tr>
          <tr>
            <td>Amortization / Casualty Loss / Depletion</td>
            <td class="sign-col add">+</td>
            <td><input type="number" id="f_amort1" value="0" oninput="calculate()"></td>
            <td><input type="number" id="f_amort2" value="0" oninput="calculate()"></td>
          </tr>
          <tr>
            <td>Business Use of Home</td>
            <td class="sign-col add">+</td>
            <td><input type="number" id="f_home1" value="0" oninput="calculate()"></td>
            <td><input type="number" id="f_home2" value="0" oninput="calculate()"></td>
          </tr>
          <tr>
            <td>Meal &amp; Entertainment Exclusion</td>
            <td class="sign-col subtract">&minus;</td>
            <td><input type="number" id="f_meals1" value="0" oninput="calculate()"></td>
            <td><input type="number" id="f_meals2" value="0" oninput="calculate()"></td>
          </tr>
        </tbody>
      </table>
    </div>

    <!-- Results -->
    <div class="calc-section">
      <h2>Results</h2>

      <div class="results-grid">
        <div class="result-card">
          <div class="result-card__label">Year 1 Total</div>
          <div class="result-card__value" id="f_total1">$0.00</div>
        </div>
        <div class="result-card">
          <div class="result-card__label">Year 2 Total</div>
          <div class="result-card__value" id="f_total2">$0.00</div>
        </div>
      </div>

      <div class="result-highlight" style="margin-top: var(--space-lg);">
        <div class="result-highlight__label">Monthly Qualifying Income</div>
        <div class="result-highlight__value" id="f_monthly">$0.00</div>
      </div>

      <div class="action-bar">
        <button type="button" class="btn btn-secondary" onclick="exportCSV()">Export CSV</button>
        <button type="button" class="btn btn-secondary" onclick="window.print()">Print</button>
        <button type="button" class="btn btn-ghost" onclick="clearAll()">Clear All</button>
      </div>
    </div>

    <%- include('../../partials/show-calculations', { calcId: 'income-schedule-f' }) %>

  </div>

  <div class="calc-page__footer">
    <div class="calc-page__footer-title">Schedule F Guidelines</div>
    <p>
      Schedule F (Form 1040) reports profit or loss from farming operations. Qualifying income
      includes net farm profit with add-backs for non-cash expenses such as depreciation,
      amortization, casualty loss, and depletion. Ongoing cooperative and CCC payments are
      included. Business use of home is added back, while the non-deductible portion of meals
      and entertainment is subtracted. Standard averaging policy applies when two years of
      returns are available.
    </p>
  </div>
</div>
